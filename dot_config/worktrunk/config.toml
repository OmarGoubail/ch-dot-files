# WorkTrunk configuration
# https://worktrunk.dev/config/

[commit]
# Default staging behavior for commits
stage = "all"

[step.copy-ignored]
# Keep per-worktree copies from pulling large editor/index caches.
exclude = [".expert/"]
